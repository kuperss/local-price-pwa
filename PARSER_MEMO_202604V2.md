# 202604V2 解析備忘

這份備忘是給之後維護 `local-price-pwa` 時使用，目標是避免再次踩到同一批 PDF 解析與 UI 回歸問題。

適用範圍：
- `LED-國內定價表-主檔(202604)V2.pdf`
- `V3_S1E ~ S5FG` 這類有浮水印的 202604 系列檔案

## 核心原則

1. 這份 PDF 雖然可搜尋，但本質上仍是排版格式，不是資料格式。
2. 不要假設同一列文字就代表同一個欄位，很多值會被拆成多段文字。
3. 不要只靠檔名判斷版型，優先依內容偵測 header 與欄位結構。
4. 任何 parser 修改都要重新驗證整包樣本，不能只看單一品項。

## 已知正確輸出規格

不管有沒有 `@` 欄，都應統一呈現成：

- `型號`
- `建議售價`
- `搭贈`
- `底價`
- `量價`
- `開盤價`
- `補充資訊`

範例：

```text
LED-E27ED6YSTR3
建議售價：135
搭贈：10+1/30+5/50+15
底價：62
量價：69
開盤價：80
補充資訊：[6.5w/2300K/760LM]
```

```text
D-E2750DR2
建議售價：960
搭贈：11+1/20+4/
底價：350
量價：400
開盤價：440
補充資訊：耐壓277v
```

```text
D-ARSP9DR5
建議售價：338
搭贈：//
底價：135
量價：155
開盤價：160
補充資訊：(D/W/N) 24°角 R9>50
```

## 最常遇到的誤區

### 1. 把 PDF 當成真正表格

錯誤觀念：
- 覺得 PDF 可搜尋，就代表可以穩定按欄位直接讀。

實際情況：
- 型號可能被拆成 `D-AR`、`SP`、`9DR5`
- 備註可能被拆成多個 row
- 同頁 header 可能分兩列
- 右側備註可能和價格欄距離很近

結論：
- 解析一定要靠座標、欄位邊界、共享欄位規則一起判斷。

### 2. 以為 header 永遠單列

錯誤觀念：
- `品編 / 建議售價 / 搭贈 / A / B / C / 備註` 一定在同一列。

實際情況：
- `備註` 常在上一列
- `A / B / C` 與 `底價 / 量價 / 開盤價` 會上下分開

正確作法：
- header 偵測要支援雙列表頭。
- 欄位對應必須固定成：
  - `底價 -> A`
  - `量價 -> B`
  - `開盤價 -> C`

### 3. 用「最後三個價格」猜 A/B/C

錯誤觀念：
- 每列最後三個數字就是 `底價 / 量價 / 開盤價`。

實際問題：
- 有時右側備註或其他數字會混進來，導致價格欄整個錯位。

正確作法：
- 一律按欄位位置讀取。
- `開盤價` 只能讀 `C` 欄，不能靠順序猜。

### 4. 右側補充資訊污染價格欄

曾發生問題：
- `100發光角160°`
- `1801560lm/100-277V`

根因：
- parser 把右側 `備註/補充資訊` 誤塞進 `開盤價`。

正確作法：
- 任何落在 `開盤價` 欄右側的內容，優先視為 `補充資訊`。
- 價格欄若混入中文或明顯非價格內容，要拆回：
  - 價格
  - 補充資訊

### 5. `@` 欄干擾建議售價

曾發生問題：
- `40135` 這種把 `@ 40` 和 `135` 黏在一起的值

正確作法：
- `有 @ 欄` 時，`建議售價` 只能讀真正的建議售價欄。
- `@` 欄是可選欄位，但不能和建議售價合併。

### 6. 搭贈是共享欄，不一定只屬於單列

曾發生問題：
- 只抓到 `30+5`
- 少掉 `50+15`
- 把後面區塊的搭贈整串接到前面

根因：
- `10+1` 可能掛在某一列
- `30+5`、`50+15` 可能是後續共享列
- 區塊切換若沒正確重置，共享搭贈會外溢

正確作法：
- 共享搭贈要能累積成完整值，例如 `10+1/30+5/50+15`
- 進入新分類區塊時，必須重置共享搭贈
- 像 `E27`、`AR-111` 這種帶數字的分類列，仍然要視為分類列

### 7. 搜尋分類 token 容易做出空值卡片

曾發生問題：
- 搜 `AR` 或 `ATR` 時，命中分類標題本身，出現空資料卡片

正確作法：
- 搜尋應以真實 entry 為主
- 不要讓純分類列直接進搜尋索引

### 8. 浮水印會直接污染欄位值

已確認的干擾形式：
- `S1J1,615`
- `S1J270`
- `S1J25`
- 單獨的 `S1J`

影響：
- 把一列拆成兩筆
- 讓價格或備註變髒
- 造成總筆數不一致

目前對策：
- 在 `cleanCellText()` 最底層就先過濾浮水印
- 目前已處理這類代碼：
  - `S1E`
  - `S1F`
  - `S1G`
  - `S1H`
  - `S1J`
  - `S2A`
  - `S2B`
  - `S3A`
  - `S3B`
  - `S3C`
  - `S5A`
  - `S5B`
  - `S5C`
  - `S5D`
  - `S5ES`
  - `S5FG`

### 9. 直接用 PowerShell 覆寫 `app.js` 有編碼風險

曾發生問題：
- 直接 `Set-Content` 後，原本中文常數與 regex 被編碼污染
- 造成整份 `app.js` 看起來像亂碼，甚至可能出現語法問題

正確作法：
- 優先用 `apply_patch`
- 若一定要腳本改檔，先確認檔案是 UTF-8，再小範圍修改
- 改完一定要做語法檢查

## 這次已確認的浮水印修正結果

資料夾：
- `D:\KupeR Drive\KupeR\VS CODE\底價表\給各區\浮水印`

樣本數：
- 16 份 PDF

修正前：
- 15 份是 `492` 筆
- `LED-國內定價表-主檔(202604)V3_S1J.pdf` 是 `493` 筆

修正後：
- 16 份全部都是 `492` 筆

另外已確認：
- `S1J` 不再殘留在解析出的 entry 內容中

## 每次修改 parser 後必做的驗證

### 最小驗證

至少檢查這幾筆：
- `LED-E27ED6YSTR3`
- `D-E2750DR2`
- `D-ARSP9DR5`

### 進階驗證

至少抽樣這幾區：
- `E27`
- `AR`
- `ATR`
- 平板燈
- 崁燈
- 軌道燈

### 浮水印驗證

至少驗：
- `S1E`
- `S1J`

確認：
- 筆數一致
- 不出現多一筆空 row
- 內容內沒有殘留 `S1J` 這類字串

## UI 相關回歸提醒

### 1. 手機版控制區很容易被長檔名撐壞

曾發生問題：
- 載入 PDF 後，上方按鈕列被往右撐出畫面

提醒：
- 手機版要用單欄堆疊
- 長檔名與狀態文字要允許換行
- 不能讓控制卡片內元素有固定最小寬度

### 2. 桌機版搜尋框容易被按鈕擠爆

曾發生問題：
- 搜尋框縮成一小條

提醒：
- 桌機版要維持：
  - 輸入框 `flex: 1`
  - 搜尋按鈕固定寬度
- 不要把手機版的全寬按鈕規則直接套到桌機版

### 3. 細節視窗要同時支援

- 內容可垂直捲動
- 下拉關閉
- 不影響內部 scroll

## 建議維護流程

1. 先用本機 PDF 重現問題
2. 不要只看單一品項，至少比對：
   - 同區塊前後幾筆
   - 同檔其他頁
   - 有無浮水印版本
3. 改 parser 後先做語法檢查
4. 再跑批次驗證
5. 最後才在手機 UI 上做人工確認

## 建議保留的實務原則

- 先修根因，不要只修畫面結果
- 先防資料污染，再談搜尋體驗
- 共享欄位一定要有區塊邊界
- 類別列、備註列、續行列要明確分開
- 所有 parser 版本變更都要 bump bundle version，避免舊索引干擾

